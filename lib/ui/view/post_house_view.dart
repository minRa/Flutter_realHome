import 'package:flutter/material.dart';

class PostHouseView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('this is post page!!!'),
    );
  }
}